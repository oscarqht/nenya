## 1. Implementation

- [x] 1.1 Remove the manifest homepage override while retaining the New Tab override.
- [x] 1.2 Add the Options-page control and its accessible status/error feedback.
- [x] 1.3 Persist and validate the configured HTTP(S) destination in extension storage.
- [x] 1.4 Replace the hard-coded and inline New Tab redirect with an external script that uses the stored destination and safe default.
- [ ] 1.5 Manually verify the default destination, saving a custom destination, invalid-input feedback, and that the manifest no longer contains a homepage override.
